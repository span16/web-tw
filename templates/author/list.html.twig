<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List of Authors</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }
        table, th, td {
            border: 1px solid #ddd;
        }
        th, td {
            padding: 12px;
            text-align: left;
        }
        th {
            background-color: #f4f4f4;
        }
        img {
            width: 100px;
            height: auto;
        }
        a {
            text-decoration: none;
            color: #007bff;
        }
        a:hover {
            text-decoration: underline;
        }
        .total-books {
            margin-top: 20px;
            font-weight: bold;
        }
    </style>
</head>
<body>

    <h1>List of Authors</h1>

    {% if authors is defined and authors|length > 0 %}
        <table>
            <thead>
                <tr>
                    <th>Picture</th>
                    <th>Username</th>
                    <th>Email</th>
                    <th>Number of Books</th>
                    <th>Details</th>
                </tr>
            </thead>
            <tbody>
                {% set total_books = 0 %}
                {% for author in authors %}
                    <tr>
                        <td><img src="{{ author.picture }}" alt="{{ author.username }}"></td>
                        <td>{{ author.username|upper }}</td>
                        <td>{{ author.email }}</td>
                        <td>{{ author.nb_books }}</td>
                        <td><a href="{{ path('author_details', {'id': author.id}) }}">Details</a></td>
                    </tr>
                    {% set total_books = total_books + author.nb_books %}
                {% endfor %}
            </tbody>
        </table>
        <p class="total-books">Total number of books by all authors: {{ total_books }}</p>
    {% else %}
        <p>No authors found.</p>
    {% endif %}

</body>
</html>
